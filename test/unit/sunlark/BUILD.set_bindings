sunlark_test(
    name = "bindings-test-1",

    bool_attr = True,

    int_attr = 3,
    int_list = [1, 2, 3],

    plaindq = "I am a plain double-quoted string",
    plainsq = 'I am a plain single-quoted string',
    rawdq = r"I am a raw double-quoted string",
    rawsq = r'I am a raw single-quoted string',
    bindq = b"I am a binary double-quoted string",
    binsq = b'I am a binary single-quoted string',
    binrawdq = br"I am a binary raw double-quoted string",
    binrawsq = br'I am a binary raw double-quoted string',

    string_list_pdq1 = ["a0", "a1", "a2"],
    string_list_bdq1 = [b"b0", b"b1", b"b2"],
    string_list_rdq1 = [r"c0", r"c1", r"c2"],

    string_list_dup = ["a", "b", "a"]
)

sunlark_test(
    name = "bindings-test-2",

    int_veca = [1, 2, 3],
    int_vecb = [
        10, 20, 30
    ],
    int_vecc = [
        100,
        200,
        300
    ]
)
